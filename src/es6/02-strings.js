let hello = 'Hello';
let world = 'World';
let epicPhrase = hello + ' ' + world;
console.error(epicPhrase);

/**
 * Template Literals
 * Pueden ser Multi-line strings
 */
let epicPhrase2 = `${hello} 
${world}`;
let multi_line = `Haciendo multi 
linea utilizando
template literals`;
console.log(multi_line);

console.log(epicPhrase2);
